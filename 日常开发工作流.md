# 日常开发工作流 · 以后代码怎么写进仓库

> **一句话答案**：代码写在 `D:\code\java-learning` 里，写完后 `git add . → git commit → git push`。
> **不是**在 GitHub 网页上敲代码，网页只用来**看**。

---

## 一、先搞清楚：哪边是"真的"

```
D:\code\java-learning\          ← 🏠 你的工作台（在 IDEA 里打开这个文件夹）
        │
        │  git push  ⬆️
        │  git pull  ⬇️
        ↓
github.com/qing070602/java-learning   ← ☁️ 云端的备份 + 给面试官看的橱窗
```

| | 本地 `D:\code\java-learning` | GitHub 网页 |
| --- | --- | --- |
| 用来干什么 | ✅ **写代码、跑代码、改代码** | 👀 看历史、看贡献图、给面试官展示 |
| 能不能写代码 | 当然能，这才是主战场 | 能，但**别这么干**（没法编译、没法调试） |
| 断网了能不能用 | ✅ 能 | ❌ 不能 |

> [!important] 记住一句话
> **网页上的代码是"推上去的"，不是"写上去的"。**
> 你在网页上改一个文件，本地就落后了，下次 push 还会冲突。**所有修改都在本地做。**

---

## 二、你要装的只有一件事：用 IDEA 打开这个文件夹

你机器上已经装好了：

```
IDEA：D:\develop\IDEA\IntelliJ IDEA 2026.1.4
JDK ：D:\develop\JDK（Java 25.0.3）
Git ：D:\Git
```

**操作（只做一次）**：

1. 打开 IDEA → `File` → `Open`
2. 选择文件夹 **`D:\code\java-learning`**（⚠️ 选这个**文件夹**，不是里面某个 `.java` 文件）
3. 弹出信任提示就点 **Trust Project**
4. 左上角项目树里能看到 `java-basics`、`algorithm` 等目录 —— 成功

> [!tip] 为什么直接打开整个仓库，而不是每章建一个工程？
> 因为你写的是**练习**（单文件、几十行），Java 25 支持**直接运行单个 .java 文件**，不需要建模块、不需要配 pom.xml。
> 一个仓库 = 一个 IDEA 工程，最简单，也不会出现"8 个窗口切来切去"的混乱。

**右击任何 `.java` 文件** → `Run 'xxx.main()'` 就能跑。IDEA 的控制台是 UTF-8，**不会有中文乱码**。

---

## 三、标准流程（每次写代码都是这 6 步）

### 第 1 步：想清楚这题是哪个章节的

看你的 Obsidian 笔记，确认现在学到第几章 → 决定代码放哪个文件夹。

```
第 4 章数组         → java-basics\01-入门语法\
方法、递归          → java-basics\02-方法\
面向对象            → java-basics\03-面向对象\
洛谷 / 牛客 / LC 题  → algorithm\对应题型\
```

### 第 2 步：新建文件（复制模板）

在 IDEA 里：
1. 右键 `java-basics\01-入门语法` → `Copy` 那个 `PracticeTemplate.java`，再 `Paste`
2. 弹出重命名框，输入 **有意义的题目名**：

```
练习-数组求最值.java
练习-数组反转.java
案例-学生成绩统计.java
```

> ⚠️ **文件名随便起，但里面的类名必须保持 `PracticeTemplate`**。
> 原因：Java 要求 `public` 类名 = 文件名，中文文件名带横线不是合法类名，所以模板故意没写 `public`。
> 改类名会报 `class XXX is public, should be declared in a file named XXX.java`。

### 第 3 步：写代码 + 填头部信息

把模板里的 6 行头部信息填掉：

```java
/*
 * 章节：04 数组
 * 知识点：数组遍历 / 求最值
 * 练习要求：键盘录入 5 个成绩，求最高分和平均分
 * 日期：2026-09-20
 * 状态：✅ 独立完成
 */
```

**`状态` 填 🔴 的，就是你该回头重做的。**

### 第 4 步：跑通它

- IDEA 里：右击 → `Run`，看输出对不对
- 一定要**自己敲一遍**，别复制老师的代码

### 第 5 步：提交（在 IDEA 里按快捷键，或开终端敲命令）

**路线 A：IDEA 里点（省事）**

| 操作 | 快捷键 | 说明 |
| --- | --- | --- |
| 提交 | `Ctrl + K` | 勾选要提交的文件，写 commit 信息，点 Commit |
| 推送 | `Ctrl + Shift + K` | 点 Push |

**路线 B：终端敲命令（推荐前两周用这个）**

```powershell
cd D:\code\java-learning
git add .
git commit -m "feat: 数组章节 - 完成求最值练习"
git push
```

> 为什么推荐先用命令行？出问题时你知道发生了什么。IDEA 的按钮报错时你只能干瞪眼。

### 第 6 步：刷新 GitHub 确认

打开 https://github.com/qing070602/java-learning —— 看到新文件 + 绿格子 +1。

---

## 四、一个真实例子：从零到推送

假设你今天写了一道"求数组最大值"的练习：

```powershell
# ① 进仓库
cd D:\code\java-learning

# ② 复制模板并改名
copy java-basics\01-入门语法\PracticeTemplate.java "java-basics\01-入门语法\练习-数组求最值.java"

# ③ 打开 IDEA 写代码、跑通（这一步在 IDEA 里做）

# ④ 看看改了哪些文件（好习惯：提交前先看一眼）
git status

# ⑤ 提交 + 推送
git add .
git commit -m "feat: 数组章节 - 完成求最值练习"
git push
```

**第二天又写了两道题**：

```powershell
cd D:\code\java-learning
git status                          # 会看到 2 个新文件
git add .
git commit -m "feat: 数组章节 - 反转与查找练习"
git push
```

**就这样。每天重复。** 你不需要学任何其他 Git 命令。

---

## 五、commit 信息怎么写（半年后你会感谢自己）

格式：**`类型: 章节 - 具体做了什么`**

```powershell
git commit -m "feat: 数组章节 - 求最值 / 反转 / 查找三题"
git commit -m "feat: 洛谷 P1001 A+B Problem"
git commit -m "fix: 修复平均分算成整数的 bug"
git commit -m "docs: 更新 README 进度看板"
```

| 前缀 | 用在 |
| --- | --- |
| `feat:` | 加了新代码 |
| `fix:` | 修了 bug |
| `docs:` | 只改文档 |

❌ **别写**：`update`、`修改`、`111`、`提交` —— 半年后你完全想不起来当时干了啥。

---

## 六、每天推几次？

| 场景 | 推不推 |
| --- | --- |
| 写完一道练习，跑通了 | ✅ **推**（哪怕只有 20 行） |
| 写到一半卡住了 | 🟡 可以推，commit 写 `wip: 数组反转写到一半卡在边界条件` |
| 改了注释、改了 README | ✅ 推，`docs:` 开头 |
| 一个功能写了两小时还没跑通 | ✅ 推，别攒着 |

> [!important] 核心原则：**每天至少 push 一次。**
> 半年后面试官点进你的仓库，他看的**不是代码量**，是 **"这半年你有没有连续在写"**。
> 一天的绿格子胜过一千行的批量上传。

---

## 七、常见情况怎么办

| 情况 | 怎么做 |
| --- | --- |
| **代码跑出中文乱码**（命令行） | `java "-Dstdout.encoding=UTF-8" PracticeTemplate`；IDEA 里不会乱码 |
| **报错 `class X is public, should be declared in a file named X.java`** | 你改了类名。把类名改回 `PracticeTemplate`，或去掉 `public` |
| **push 报 `failed to push some refs`** | 先 `git pull --no-rebase` 再 `git push` |
| **提交错了想撤销** | `git reset --soft HEAD~1`（撤 commit，保留代码） |
| **不小心提交了不该传的文件** | 告诉我，别自己乱操作。**如果传了密码，立刻去平台改密码** |
| **想看看自己改了什么** | `git status`（改了哪些文件）、`git diff`（改了哪些行） |
| **忘了上次推到哪** | `git log --oneline -5` |

---

## 八、每周日花 5 分钟做的两件事

1. **更新 README 的进度看板** —— 算法题数、章节进度，如实填
2. **回看 `git log --oneline`** —— 这一周你到底写了什么？

```
9aaf338 docs: 新增练习代码规范与练习模板
e967d28 docs: 推送指南改为 GitHub 版
7fefef3 init: 仓库初始化，建立目录结构
```

> 如果一周下来 `git log` 只有一两条，**说明你这周没怎么练** —— 这是最诚实的一面镜子。

---

## 九、把这句话贴在显示器上

> **写代码 → 跑通 → `git add .` → `git commit -m "feat: xxx"` → `git push`**
>
> **一天一次，坚持半年。**

那时候你简历上会有：
- 一个连续提交 180 天的 GitHub 账号
- `algorithm/` 里 200 多道带思路注释的题
- `projects/` 里 3 个能演示的项目

**这就是你二本学历之外，唯一能拿得出手的东西。**

---

*相关文档：[GitHub推送指南.md](GitHub推送指南.md) ｜ [java-basics/练习代码规范.md](java-basics/练习代码规范.md) ｜ [Git安装说明.md](Git安装说明.md)*
